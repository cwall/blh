from django.conf.urls import url
from django.contrib import admin
from page import views

urlpatterns = [
    # url(r'^$', PageListView.as_view(), name='page-list'),
    #url(r'^(?P<page_slug>[\w-]+)/$', views.detail, name='detail'),
]